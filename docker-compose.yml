services:
  postgres:
    image: postgres:17-alpine
    container_name: running-challenge-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    env_file:
      - .env.docker
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
